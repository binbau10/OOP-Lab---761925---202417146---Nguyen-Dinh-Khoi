If you create a constructor method to build a DVD by title then create a constructor method to build
a DVD by category. Does JAVA allow you to do this?

No, because both constructors have the same signature. They have the same name and their parameter is String. The name of parameters (such as title or category) is not included in a signature

Try to add a method addDigitalVideoDisc which allows to pass an arbitrary number of arguments for dvd. Compared to an array parameter. What do you prefer in this case?

I prefer the Varargs method because the number of arguments is small (The method terminates early without iterating through the Varargs if the number of arguments is greater than 20) and it does not require initializing an array.

After the call of swap(jungleDVD, cinderellaDVD) why does the title of these two objects still remain?

Because the method only got the copied values (including references) of the objects. The copy also pointed to the same data region in the heap memory, and the method swapped their reference. The original objects still pointed to the original data region.

After the call of changeTitle(jungleDVD, cinderellaDVD.getTitle()) why is the title of the JungleDVD changed?

The method got a copy of JungleDVD which points to the region with title = "Jungle". It used the copied value (reference) to call setTitle which changed the refered object's title into "Cinderella". The later assignment only changed the reference, and the object was unchanged, keeping the title "Cinderella".
-----------------
Which classes are aggregates of other classes?
Cart and Store are aggregates of ArrayList.

If the passing object is not an instance of Media, what happens?
The casting from Object to Media would fails, causing an error.

What class should implement the Comparable interface? 
Media
In those classes, how should you implement the compareTo() method to reflect the ordering that we want?
compareTo() method take one Media object as argument and return an int determining the comparison result.

Can we have two ordering rules of the item (by title then cost and by cost then title) if we use this Comparable interface approach?
No. Each class can only have one method with the same signature, so there can only be one compareTo() method.

Suppose the DVDs have a different ordering rule from the other media types, that is by title, then decreasing length, then cost. How would you modify your code to allow this?
We would have to override the compareTo() method in the DigitalVideoDisc class, and the comparison only works with Collection os DigitalVideoDisc type.